# Application name
spring.application.name=swasth-id-backend

# Server port
server.port=8081

# Datasource configuration (example for MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/swasth_id_db
spring.datasource.username=root
spring.datasource.password=DTap@0388
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Logging
logging.level.org.springframework=INFO
logging.level.com.yourpackage=DEBUG

# Security (optional, if using Spring Security)
# spring.security.user.name=admin
# spring.security.user.password=admin123
